const router=require("express").Router();const c=require("../controllers/serviceController");const {protect,authorize}=require("../middlewares/authMiddleware");router.get("/",c.list);router.get("/:slug",c.get);router.post("/",protect,authorize("admin","editor"),c.create);router.patch("/:id",protect,authorize("admin","editor"),c.update);router.delete("/:id",protect,authorize("admin"),c.remove);module.exports=router;
