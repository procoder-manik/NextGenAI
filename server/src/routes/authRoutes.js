const router=require("express").Router();const c=require("../controllers/authController");const validate=require("../middlewares/validateRequest");const {registerSchema,loginSchema}=require("../validators/authValidator");const {protect,authorize}=require("../middlewares/authMiddleware");const {authLimiter}=require("../middlewares/rateLimiter");router.post("/register",authLimiter,protect,authorize("admin"),validate(registerSchema),c.register);router.post("/login",authLimiter,validate(loginSchema),c.login);router.get("/me",protect,c.me);module.exports=router;
