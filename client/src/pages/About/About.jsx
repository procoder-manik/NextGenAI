import PageHero from "../../components/common/PageHero";
import Section from "../../components/ui/Section";
import Button from "../../components/ui/Button";
import SEO from "../../seo/SEO";
import { stats, team, values } from "../../data/siteData";
export default function About() { return <><SEO title="About" description="Meet the people and principles behind NextGenAI." path="/about" /><PageHero eyebrow="About us" title="We help good businesses become impossible to ignore." description="NextGenAI is a digital growth partner for teams ready to make their next move count." /><Section><div className="grid gap-10 lg:grid-cols-2"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-600">Our story</p><h2 className="mt-4 text-3xl font-bold">A partner built for progress.</h2></div><div className="space-y-5 leading-8 text-slate-600"><p>We started with a simple belief: the best digital work is clear, useful, and connected to a real goal. That belief still guides every engagement.</p><p>Today, our strategists, designers, and engineers help ambitious organisations turn complex opportunities into focused momentum.</p></div></div></Section><Section className="bg-slate-50"><div className="grid gap-6 md:grid-cols-2"><article className="rounded-2xl bg-white p-8"><h2 className="text-2xl font-bold">Our mission</h2><p className="mt-4 leading-7 text-slate-600">Give ambitious teams the strategy and digital capability to grow with confidence.</p></article><article className="rounded-2xl bg-slate-950 p-8 text-white"><h2 className="text-2xl font-bold">Our vision</h2><p className="mt-4 leading-7 text-slate-300">A future where useful technology makes every customer experience more human.</p></article></div></Section><Section><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-600">Our values</p><div className="mt-7 grid gap-5 md:grid-cols-3">{values.map((value) => <article key={value.title} className="rounded-2xl border p-7"><h2 className="text-xl font-bold">{value.title}</h2><p className="mt-3 leading-7 text-slate-600">{value.text}</p></article>)}</div></Section><Section className="bg-slate-50"><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-600">The team</p><div className="mt-7 grid gap-5 md:grid-cols-3">{team.map((person) => <article key={person.name} className="rounded-2xl bg-white p-7"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-blue-100 font-bold text-blue-700">{person.initials}</div><h2 className="mt-5 text-xl font-bold">{person.name}</h2><p className="mt-1 text-slate-600">{person.role}</p></article>)}</div></Section><Section><div className="grid gap-6 text-center sm:grid-cols-4">{stats.map((stat) => <div key={stat.label}><p className="text-3xl font-bold text-blue-700">{stat.value}</p><p className="mt-1 text-sm text-slate-600">{stat.label}</p></div>)}</div><div className="mt-16 text-center"><h2 className="text-3xl font-bold">Ready to build something meaningful?</h2><Button to="/contact" className="mt-6">Start a conversation</Button></div></Section></>; }
