import { Link } from "react-router-dom";
import PageHero from "../../components/common/PageHero";
import FaqAccordion from "../../components/common/FaqAccordion";
import Section from "../../components/ui/Section";
import Button from "../../components/ui/Button";
import SEO from "../../seo/SEO";
import { faqs, processSteps, services } from "../../data/siteData";
export default function Services() { return <><SEO title="Services" description="Strategy, design, technology, and growth services from NextGenAI." path="/services" /><PageHero eyebrow="Services" title="The capability to turn ambition into action." description="Bring us a challenge, a goal, or an opportunity. We’ll help you shape the next move." /><Section><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.map((service) => <article key={service.slug} className="rounded-2xl border border-slate-200 p-7 transition hover:-translate-y-1 hover:shadow-lg"><span className="text-sm font-bold text-blue-600">{service.icon}</span><h2 className="mt-6 text-xl font-bold">{service.title}</h2><p className="mt-3 leading-7 text-slate-600">{service.description}</p><ul className="mt-5 space-y-2 text-sm text-slate-600">{service.features.map((feature) => <li key={feature}>✓ {feature}</li>)}</ul><Link className="mt-6 inline-block font-semibold text-blue-700" to={`/services/${service.slug}`}>Explore service →</Link></article>)}</div></Section><Section className="bg-slate-50"><div className="grid gap-8 lg:grid-cols-2"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-600">What to expect</p><h2 className="mt-4 text-3xl font-bold">A flexible team, a disciplined process.</h2><p className="mt-4 leading-7 text-slate-600">We bring the right mix of strategy, craft, and technology to the work in front of you.</p></div><div className="grid gap-3 sm:grid-cols-2">{processSteps.map((step) => <div key={step.number} className="rounded-xl bg-white p-5"><p className="font-bold text-blue-600">{step.number}</p><h3 className="mt-3 font-bold">{step.title}</h3></div>)}</div></div></Section><Section><div className="rounded-3xl border border-dashed border-slate-300 p-8 text-center"><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-600">Flexible engagements</p><h2 className="mt-3 text-3xl font-bold">Pricing shaped around your goals.</h2><p className="mx-auto mt-4 max-w-xl leading-7 text-slate-600">Every project is different. We’ll recommend a focused scope and clear investment after learning what matters most.</p><Button to="/contact" className="mt-6">Request a proposal</Button></div></Section><Section className="bg-slate-50"><div className="mx-auto max-w-3xl"><h2 className="mb-7 text-3xl font-bold">Service FAQs</h2><FaqAccordion items={faqs} /></div></Section></>; }
