import { Link, useParams } from "react-router-dom";
import PageHero from "../../components/common/PageHero";
import Section from "../../components/ui/Section";
import Button from "../../components/ui/Button";
import SEO from "../../seo/SEO";
import { processSteps, services } from "../../data/siteData";
export default function ServiceDetails() { const { slug } = useParams(); const service = services.find((item) => item.slug === slug); if (!service) return <section className="p-12 text-center"><h1 className="text-3xl font-bold">Service not found</h1><Link to="/services" className="mt-5 inline-block text-blue-700">Back to services</Link></section>; return <><SEO title={service.title} description={service.description} path={`/services/${service.slug}`} /><PageHero eyebrow="Service" title={service.title} description={service.description}><Button to="/contact" className="mt-8">Discuss your project</Button></PageHero><Section><div className="grid gap-10 lg:grid-cols-[1.4fr_.6fr]"><div><h2 className="text-3xl font-bold">A tailored approach to {service.title.toLowerCase()}.</h2><p className="mt-5 leading-8 text-slate-600">We start with the context behind the request, then combine the right expertise into a plan your team can act on. The result is work that is purposeful, polished, and ready to perform.</p><h3 className="mt-10 text-xl font-bold">What’s included</h3><ul className="mt-5 grid gap-3 sm:grid-cols-2">{service.features.map((feature) => <li key={feature} className="rounded-lg bg-slate-50 p-4 font-medium">✓ {feature}</li>)}</ul></div><aside className="rounded-2xl bg-slate-950 p-7 text-white"><p className="text-sm font-bold text-cyan-300">Need a custom scope?</p><p className="mt-4 text-slate-300">We’ll build an engagement around the outcome you need.</p><Button to="/contact" variant="secondary" className="mt-6">Get in touch</Button></aside></div></Section><Section className="bg-slate-50"><h2 className="mb-8 text-3xl font-bold">How we deliver</h2><div className="grid gap-5 md:grid-cols-4">{processSteps.map((step) => <div key={step.number}><p className="font-bold text-blue-600">{step.number}</p><h3 className="mt-3 font-bold">{step.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{step.text}</p></div>)}</div></Section></>; }
