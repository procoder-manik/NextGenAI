import { Link, useParams } from "react-router-dom";
import PageHero from "../../components/common/PageHero";
import Section from "../../components/ui/Section";
import SEO from "../../seo/SEO";
import { posts } from "../../data/siteData";
export default function BlogDetails() { const { slug } = useParams(); const post = posts.find((item) => item.slug === slug); if (!post) return <section className="p-12 text-center"><h1 className="text-3xl font-bold">Article not found</h1><Link to="/blog" className="mt-4 inline-block text-blue-700">Back to insights</Link></section>; const related = posts.filter((item) => item.slug !== slug).slice(0, 2); return <><SEO title={post.title} description={post.excerpt} path={`/blog/${post.slug}`} /><PageHero eyebrow={post.category} title={post.title} description={post.excerpt}><p className="mt-6 text-sm text-slate-300">By {post.author} · {post.date} · {post.readTime}</p></PageHero><Section><div className="grid gap-10 lg:grid-cols-[220px_1fr]"><aside className="text-sm"><p className="font-bold">On this page</p><ol className="mt-3 space-y-2 text-slate-600"><li>1. The opportunity</li><li>2. A practical approach</li><li>3. What to do next</li></ol><p className="mt-8 font-bold">Share</p><div className="mt-3 flex gap-2"><button className="rounded border px-3 py-2">in</button><button className="rounded border px-3 py-2">X</button><button className="rounded border px-3 py-2">↗</button></div></aside><article className="max-w-3xl"><div className="aspect-[16/8] rounded-2xl bg-gradient-to-br from-sky-200 to-indigo-200" /><div className="mt-10 space-y-7 leading-8 text-slate-700"><p>Digital growth is not about adding technology for its own sake. It is about finding the moments where a better experience, a clearer message, or a smarter system can create real value.</p><h2 className="text-2xl font-bold text-slate-900">Start with the opportunity</h2><p>Look closely at the journey your customer takes today. Where is there friction? Where do questions repeat? Those are often the best places to start.</p><h2 className="text-2xl font-bold text-slate-900">Make the next step practical</h2><p>Choose a focused experiment, decide how success will be measured, and bring the people who will use it into the process early.</p></div></article></div></Section><Section className="bg-slate-50"><h2 className="mb-7 text-2xl font-bold">Keep reading</h2><div className="grid gap-5 md:grid-cols-2">{related.map((item) => <Link key={item.slug} to={`/blog/${item.slug}`} className="rounded-xl bg-white p-6 transition hover:shadow-md"><p className="text-sm font-semibold text-blue-700">{item.category}</p><h3 className="mt-2 text-xl font-bold">{item.title}</h3></Link>)}</div></Section></>; }
