import { Link } from "react-router-dom"; import Section from "../ui/Section"; import SectionIntro from "./SectionIntro"; import { posts } from "../../data/siteData";
export default function LatestBlogs() { return <Section><div className="flex flex-wrap items-end justify-between"><SectionIntro eyebrow="From the journal" title="Ideas for building better digital growth." /><Link to="/blog" className="mb-12 font-semibold text-blue-700">View all articles →</Link></div><div className="grid gap-5 md:grid-cols-3">{posts.map((post) => <article key={post.slug} className="group"><div className="aspect-[16/10] rounded-2xl bg-gradient-to-br from-sky-100 to-indigo-100" /><p className="mt-5 text-sm font-semibold text-blue-700">{post.category}</p><h3 className="mt-2 text-xl font-bold group-hover:text-blue-700"><Link to={`/blog/${post.slug}`}>{post.title}</Link></h3><p className="mt-3 text-sm text-slate-500">{post.date} · {post.readTime}</p></article>)}</div></Section>; }
