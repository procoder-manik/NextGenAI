import { Link } from "react-router-dom"; import Section from "../ui/Section"; import SectionIntro from "./SectionIntro"; import { services } from "../../data/siteData";
export default function ServicesPreview() { return <Section className="bg-slate-50"><div className="flex flex-wrap items-end justify-between gap-4"><SectionIntro eyebrow="Capabilities" title="The skills to move your business forward." /><Link className="mb-12 font-semibold text-blue-700" to="/services">View all services →</Link></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.slice(0, 3).map((service) => <article key={service.slug} className="rounded-2xl border border-slate-200 bg-white p-7 transition hover:-translate-y-1 hover:shadow-xl"><span className="text-sm font-bold text-blue-600">{service.icon}</span><h3 className="mt-7 text-xl font-bold">{service.title}</h3><p className="mt-3 leading-7 text-slate-600">{service.description}</p><Link className="mt-6 inline-block font-semibold" to={`/services/${service.slug}`}>Learn more →</Link></article>)}</div></Section>; }
