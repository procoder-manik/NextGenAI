import { useState } from "react"; import Section from "../ui/Section";
export default function Newsletter() { const [submitted, setSubmitted] = useState(false); return <Section className="bg-slate-100"><div className="mx-auto max-w-2xl text-center"><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-600">Stay in the loop</p><h2 className="mt-3 text-3xl font-bold">Useful ideas, delivered occasionally.</h2><p className="mt-3 text-slate-600">One thoughtful note on digital growth. No noise.</p>{submitted ? <p className="mt-6 font-semibold text-emerald-700">Thanks—you’re on the list.</p> : <form className="mx-auto mt-7 flex max-w-md gap-2" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }}><label className="sr-only" htmlFor="newsletter-email">Email address</label><input id="newsletter-email" required type="email" placeholder="you@company.com" className="min-w-0 flex-1 rounded-lg border border-slate-300 bg-white px-4 py-3" /><button className="rounded-lg bg-slate-900 px-5 py-3 font-semibold text-white hover:bg-slate-700">Subscribe</button></form>}</div></Section>; }
