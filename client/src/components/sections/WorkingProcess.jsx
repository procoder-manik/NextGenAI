import Section from "../ui/Section"; import SectionIntro from "./SectionIntro"; import { processSteps } from "../../data/siteData";
export default function WorkingProcess() { return <Section><SectionIntro eyebrow="How we work" title="A simple process, built around progress." /><div className="grid gap-5 md:grid-cols-4">{processSteps.map((step) => <article key={step.number} className="border-t-2 border-blue-600 pt-5"><p className="text-sm font-bold text-blue-600">{step.number}</p><h3 className="mt-5 text-xl font-bold">{step.title}</h3><p className="mt-3 leading-7 text-slate-600">{step.text}</p></article>)}</div></Section>; }
